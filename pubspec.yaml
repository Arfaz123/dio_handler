name: dio_handler
description: DioHandler provides utility functions and customization options to streamline API request handling in your Flutter applications.
version: 0.0.2
homepage: https://linktr.ee/arfaz_860
repository: https://github.com/Arfaz123/dio_handler.git
topics: [dio, api-call, dio-handler, http]

screenshots:
  - description: 'DioHandler Essential Flutter Package.'
    path: preview/dio_handler_preview_image.png

environment:
  sdk: '>=3.1.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  connectivity_plus: ^5.0.1
  dio: ^5.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:

#  assets:
#    - assets/images/
#    - assets/icons/
