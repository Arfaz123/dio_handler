name: dio_handler
description: DioHandler is a Flutter package designed to simplify network API calls using the Dio HTTP client. It provides utility functions and customization options to streamline API request handling in your Flutter applications.
version: 0.0.1-beta-3
homepage: https://arfaz.tech
repository: https://github.com/Arfaz123/dio_handler.git

environment:
  sdk: '>=3.1.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  connectivity_plus: ^5.0.0
  dio: ^5.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:

#  assets:
#    - assets/images/
#    - assets/icons/
